# Windows Installer Build

1. Run `scripts\build_windows.bat`.
2. Use Inno Setup or NSIS to package `dist\MediaHubDownloader` into an installer.
3. Store final artifacts under `deployment/windows/artifacts`.
